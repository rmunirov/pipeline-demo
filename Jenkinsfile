pipeline {

    agent any

    stages {

        stage('Checkout Info') {
            steps {

                sh 'pwd'
                sh 'ls -la'
            }
        }

        stage('Build') {
            steps {
                echo 'Build started'
            }
        }
    }
}