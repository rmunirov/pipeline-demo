{
  "name": "demo-app",
  "version": "1.0.0",
  "scripts": {
    "test": "echo tests passed",
    "build": "mkdir -p dist && echo build > dist/build.txt",
	"test": "jest --ci --reporters=default --reporters=jest-junit"
  },
  "devDependencies": {
    "jest": "^30.0.0",
    "jest-junit": "^16.0.0",
	"eslint": "^10.4.1"
  }
}